package org.poo.bank.entity.transaction;

public class BaseTransaction extends Transaction {
    public BaseTransaction(String description, Integer timestamp) {
        super(description, timestamp);
    }
}
